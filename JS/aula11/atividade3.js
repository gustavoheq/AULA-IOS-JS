// Dado o array fruits = ['apple', 'banana', 'orange', 'kiwi'], utilize o método
// forEach para exibir cada fruta no console.

let frutas = ['apple', 'banana', 'orange', 'kiwi']

frutas.forEach((array, index)=> {
    console.log(`A fruta ${index+1} é: ${array}`);
    console.log(`--- --- ---`);
})
